---
import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import { ctfs, ctfStats } from '../data/ctfs';

const posts = await getCollection('blog');

// Resolve writeup posts
function getWriteupPosts(writeupIds: string[]) {
    return writeupIds
        .map(id => posts.find(p => p.id === id))
        .filter(Boolean);
}
---

<Layout title="CTFs | idktheflag">
    <main class="container">
        <h1>CTF History</h1>
        <div class="stats-bar">
            <div class="stat">
                <span class="stat-label">Overall Rating</span>
                <span class="stat-value">#{ctfStats.overallPlace}</span>
            </div>
            <div class="stat">
                <span class="stat-label">Country (US)</span>
                <span class="stat-value">#{ctfStats.countryPlace}</span>
            </div>
            <div class="stat">
                <span class="stat-label">Rating Points</span>
                <span class="stat-value">{ctfStats.ratingPoints}</span>
            </div>
            <div class="stat">
                <span class="stat-label">Year</span>
                <span class="stat-value">{ctfStats.year}</span>
            </div>
        </div>
        <p class="ctftime-link">
            full stats on <a href="https://ctftime.org/team/419270" target="_blank">CTFtime</a>
        </p>

        <div class="ctf-table-wrapper">
            <table class="ctf-table">
                <thead>
                    <tr>
                        <th>Place</th>
                        <th>Event</th>
                        <th>CTF Points</th>
                        <th>Rating Points</th>
                        <th>Writeups</th>
                    </tr>
                </thead>
                <tbody>
                    {ctfs.map(ctf => {
                        const writeupPosts = getWriteupPosts(ctf.writeups);
                        return (
                            <tr>
                                <td class="place" data-label="Place: ">{typeof ctf.place === 'number' ? `#${ctf.place}` : ctf.place}</td>
                                <td class="event" data-label="">{ctf.name}{ctf.note && <span class="note"> ({ctf.note})</span>}</td>
                                <td data-label="CTF Points: ">{ctf.ctfPoints !== null ? ctf.ctfPoints.toLocaleString() : <span class="no-writeup">-</span>}</td>
                                <td data-label="Rating: ">{ctf.ratingPoints !== null ? ctf.ratingPoints : <span class="no-writeup">-</span>}</td>
                                <td class={`writeups-cell ${writeupPosts.length === 0 ? 'empty' : ''}`} data-label="Writeups: ">
                                    {writeupPosts.length > 0 ? (
                                        writeupPosts.map(p => (
                                            <a href={`/blog/${p!.id}/`}>{p!.data.title}</a>
                                        ))
                                    ) : (
                                        <span class="no-writeup">-</span>
                                    )}
                                </td>
                            </tr>
                        );
                    })}
                </tbody>
            </table>
        </div>
    </main>
</Layout>

<style>
    .container {
        max-width: 1100px;
        margin: 0 auto;
        padding: 2rem 1rem;
    }

    .stats-bar {
        display: flex;
        gap: 1.5rem;
        flex-wrap: wrap;
        margin: 1.5rem 0;
        padding: 1rem 1.5rem;
        background-color: #161b22;
        border: 1px solid #30363d;
        border-left: 4px solid var(--accent);
        border-radius: 4px;
    }

    .stat {
        display: flex;
        flex-direction: column;
        gap: 0.2rem;
    }

    .stat-label {
        font-size: 0.8em;
        color: #8b949e;
        font-family: monospace;
        text-transform: uppercase;
    }

    .stat-value {
        font-size: 1.3em;
        font-weight: bold;
        color: var(--accent);
        font-family: monospace;
    }

    .ctftime-link {
        color: #8b949e;
        margin-bottom: 2rem;
    }

    .ctf-table-wrapper {
        overflow-x: auto;
    }

    .ctf-table {
        width: 100%;
        border-collapse: collapse;
        font-family: monospace;
        font-size: 0.95em;
    }

    .ctf-table th {
        text-align: left;
        padding: 0.6rem 0.8rem;
        border-bottom: 2px solid var(--accent);
        color: var(--accent);
        font-size: 0.85em;
        text-transform: uppercase;
        white-space: nowrap;
    }

    .ctf-table td {
        padding: 0.6rem 0.8rem;
        border-bottom: 1px solid #21262d;
        white-space: nowrap;
    }

    .ctf-table tr:hover td {
        background-color: #161b22;
    }

    .place {
        color: var(--accent);
        font-weight: bold;
        white-space: nowrap;
    }

    .event {
        font-weight: bold;
        color: var(--text-heading);
        white-space: nowrap;
    }

    .writeups-cell {
        white-space: normal;
    }

    .writeups-cell a {
        display: block;
        font-size: 0.9em;
    }

    .no-writeup {
        color: #484f58;
    }

    .note {
        color: #8b949e;
        font-size: 0.85em;
        font-weight: normal;
    }

    @media (max-width: 720px) {
        .stats-bar {
            gap: 1rem;
        }

        .stat-value {
            font-size: 1.1em;
        }

        .ctf-table thead {
            display: none;
        }

        .ctf-table,
        .ctf-table tbody,
        .ctf-table tr,
        .ctf-table td {
            display: block;
        }

        .ctf-table tr {
            padding: 0.8rem 0;
            border-bottom: 1px solid #21262d;
        }

        .ctf-table td {
            padding: 0.15rem 0;
            border: none;
        }

        .ctf-table td::before {
            content: attr(data-label);
            font-size: 0.8em;
            color: #8b949e;
        }

        .event {
            font-size: 1.05em;
            margin-bottom: 0.15rem;
            order: -1;
        }

        .writeups-cell.empty {
            display: none;
        }

        .no-writeup {
            display: none;
        }
    }
</style>
